<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.jiahuan.androidlearn.example">

    <application>

        <activity android:name=".ExampleActivity" />

        <activity
            android:name=".function.threadpool.ThreadPoolActivity"
            android:label="@string/module_example_function_title_thread_pool" />
        <activity android:name=".function.pullrefresh.PullRefreshActivity" />
        <activity android:name=".function.eventdispatch.EventDispatchActivity" />
        <activity android:name=".function.aidl.AidlActivity" />
        <activity android:name=".function.jni.JNIActivity" />

        <activity android:name=".ui.recyclerview.RecyclerViewActivity" />

        <activity android:name=".ui.scroller.ScrollerActivity" />

        <service
            android:name=".function.aidl.RemoteOrLocalService"
            android:exported="true"
            android:process=":remote">
            <intent-filter>
                <action android:name="me.jiahuan.androidlearn.example.function.aidl.remote" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

    </application>
</manifest>
